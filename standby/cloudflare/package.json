{
  "name": "anon-music-cf-failover",
  "private": true,
  "scripts": {
    "deploy:worker": "wrangler deploy",
    "deploy:pages": "wrangler pages deploy ./public --project-name=anon-music-standby --commit-dirty=true",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.20.0"
  }
}
